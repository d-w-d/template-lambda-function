{
  "name": "lambda-s3-writer",
  "version": "1.0.0",
  "description": "AWS Lambda function to write to S3 and return URLs",
  "main": "dist/index.js",
  "scripts": {
    "test": "tsc && jest",
    "build:docker": "./_build_docker",
    "build:package": "./_build_package",
    "build": "./_build",
    "deploy": "./_deploy",
    "destroy": "./_destroy"
  },
  "dependencies": {
    "aws-sdk": "^2.1231.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.83",
    "@types/jest": "^27.0.2",
    "@types/node": "^16.10.3",
    "dotenv": "^16.4.7",
    "dotenv-cli": "^4.1.1",
    "jest": "^27.2.5",
    "ts-jest": "^27.0.5",
    "typescript": "^4.4.3"
  }
}
